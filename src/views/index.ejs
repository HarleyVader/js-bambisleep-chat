<!DOCTYPE html>
<html lang="en">
<%- include('../views/partials/head.ejs') %>
  <title>BambiSleep.Chat AIGF</title>
<style>
  /* Force system controls visibility */
  .system-controls-container {
    display: block !important;
    visibility: visible !important;
    min-height: 150px !important;
    opacity: 1 !important;
    margin-top: 20px;
    border: 1px solid rgba(255, 0, 128, 0.5);
  }
  
  .system-controls {
    display: block !important;
    visibility: visible !important;
  }
  
  .xp-progress-container {
    display: flex !important;
    visibility: visible !important;
    min-height: 30px;
    margin-top: 10px;
  }
  
  /* Ensure control panels are visible */
  #console, #buttons {
    display: block !important;
    min-height: 40px;
  }
  
  /* Diagnostic outline to see what's where */
  #console {
    border: 1px dashed rgba(0, 255, 255, 0.3);
  }
  
  #buttons {
    border: 1px dashed rgba(255, 0, 255, 0.3);
  }
</style>
  <body data-username="<%= locals.username || '' %>">
    <%- include('../views/partials/nav.ejs') %>
      <div id="chat-container">
        <!-- Add modal for username prompt -->
        <div id="username-modal" class="modal">
          <div class="modal-content">
            <p>give me your bambi name:</p>
            <input type="text" id="username-input" placeholder="Enter username">
            <button id="username-submit">Submit</button>
          </div>
        </div>
        <div id="user-input">
          <h1>BambiSleep AIGF 👁️</h1>
          <div id="llm">
            <form id="llm-form">
              <textarea id="textarea" oninput="autoExpand(this)" placeholder="Prompt my AIGF"></textarea>
              <button id="submit" type="submit" class="send-btn">Send</button>
            </form>
            <div id="user-prompt"></div>
            <div id="audiomessage">
              <audio id="audio" hidden controls></audio>
              <p id="message"></p>
              <p id="error-message" style="display: none; color: red;"></p>
            </div>
          </div>
          <br>
          <div id="system-controls-container" class="system-controls-container">
            <!-- System controls will be dynamically generated -->
          </div>
        </div>
        <div id="response-container">
          <div id="response-chat-wrapper">
            <div id="response"></div>
            <div id="chat">
              <form id="chat-form">
                <textarea id="textarea-chat" oninput="autoExpand(this)" placeholder="Send in Chat"></textarea>
                <button id="send" type="submit" class="send-btn">Chat</button>
              </form>
              <!-- For existing messages in the initial page load -->
              <ul id="chat-response">
                <% if (chatMessages && chatMessages.length> 0) { %>
                  <% chatMessages.forEach(function(msg) { %>
                    <li <% if (locals.username) { %>class="neon-glow"<% } %>>
                        <span class="chat-time">
                          <%= new Date(msg.timestamp).toLocaleTimeString([], {hour12: false}) %>
                        </span> -
                        <span class="chat-username"><a href="/profile/<%= msg.username %>" class="username-link">
                            <%= msg.username %>
                          </a>:</span>
                        <span class="chat-message">
                          <%= msg.data %>
                        </span>
                    </li>
                    <% }); %>
                      <% } %>
              </ul>
            </div>
          </div>
          <div id="eyeCursorContainer">
            <div id="eyeCursor">
              <div id="eyeCursorText"></div>
              <div id="eyeCursorText2"></div>
              <div id="eye" aria-label="Eye Cursor"></div>
              <div id="eyeCursorText3"></div>
              <div id="eyeCursorText4"></div>
            </div>
          </div>
        </div>
        <div id="collar-container" style="display: none;">
          <div id="textarea-collar-response"></div>
        </div>
      </div>
      <%- include('../views/partials/footer.ejs') %>

        <script src="js/aigf-core.js"></script>
        <script src="js/psychodelic-trigger-mania.js"></script>
        <script src="js/text2speech.js"></script>
        <script src="js/responsive.js"></script>

        <script>
          document.addEventListener("DOMContentLoaded", () => {
            // Initialize system controls
            generateSystemControls();

            // Check for username and handle modal display
            function handleUsernameCheck() {
              try {
                const modal = document.getElementById('username-modal');
                if (!modal) return;
                
                // Get username from cookie utils or fallback
                let username = '';
                if (window.cookieUtils) {
                  username = window.cookieUtils.getBambiName();
                } else {
                  // Fallback if cookieUtils not available
                  const cookies = document.cookie.split(';').reduce((acc, cookie) => {
                    const [key, value] = cookie.trim().split('=');
                    acc[key] = value;
                    return acc;
                  }, {});
                  username = cookies.bambiname || '';
                }
                
                // Store username in window for global access
                window.username = username;
                
                // Show modal if username not set or is default
                if (!username || username === 'anonBambi') {
                  modal.style.display = 'block';
                } else {
                  // If username is set and not default, load profile
                  if (username !== 'anonBambi' && window.loadProfileTriggers) {
                    window.loadProfileTriggers(username);
                  }
                }
                
                // Update body attribute
                document.body.setAttribute('data-username', username || '');
                
                // Set up submit button handler
                const submitButton = document.getElementById('username-submit');
                if (submitButton) {
                  submitButton.addEventListener('click', () => {
                    const input = document.getElementById('username-input');
                    const newUsername = input.value.trim();
                    
                    if (newUsername) {
                      // Use cookieUtils if available, otherwise set directly
                      if (window.cookieUtils) {
                        window.cookieUtils.setBambiName(newUsername);
                      } else {
                        document.cookie = `bambiname=${encodeURIComponent(newUsername)}; path=/`;
                      }
                      
                      // Update global reference
                      window.username = newUsername;
                      
                      // Notify socket if connected
                      if (window.socket && window.socket.connected) {
                        window.socket.emit('set username', newUsername);
                      }
                      
                      // Hide modal
                      modal.style.display = 'none';
                      
                      // Reload to refresh everything with new username
                      window.location.reload();
                    }
                  });
                }
              } catch (error) {
                console.error('Error in username handling:', error);
              }
            }

            // Give a short delay to ensure cookieUtils is loaded
            setTimeout(handleUsernameCheck, 100);

            // Set up chat form submission
            document.getElementById("chat-form").addEventListener("submit", (event) => {
              event.preventDefault();
              const messageInput = document.getElementById("textarea-chat");
              const message = messageInput.value.trim();

              if (message && window.username) {
                if (window.socket && window.socket.connected) {
                  window.socket.emit("chat message", {
                    data: message,
                    username: window.username
                  });

                  messageInput.value = "";
                  messageInput.style.height = 'inherit';
                } else {
                  console.error("Socket connection not available");
                  alert("Not connected to chat server. Please refresh the page.");
                }
              }
            });

            // Handle chat messages
            if (window.socket) {
              window.socket.on("chat message", function (msg) {
                // Create new chat message element
                const item = document.createElement("li");

                const timeSpan = document.createElement("span");
                timeSpan.className = "chat-time";
                timeSpan.textContent = new Date(msg.timestamp).toLocaleTimeString([], { hour12: false });

                const usernameSpan = document.createElement("span");
                usernameSpan.className = "chat-username";

                const usernameLink = document.createElement("a");
                usernameLink.href = `/profile/${msg.username}`;
                usernameLink.className = "username-link";
                usernameLink.textContent = msg.username;

                usernameSpan.appendChild(usernameLink);

                const messageSpan = document.createElement("span");
                messageSpan.className = "chat-message";
                messageSpan.textContent = msg.data;

                item.appendChild(timeSpan);
                item.appendChild(document.createTextNode(" - "));
                item.appendChild(usernameSpan);
                item.appendChild(document.createTextNode(": "));
                item.appendChild(messageSpan);

                if (window.username) {
                  item.classList.add("neon-glow");
                }

                const chatList = document.getElementById("chat-response");
                chatList.appendChild(item);
                chatList.scrollTop = chatList.scrollHeight;
              });
            }
          });

          // Generate system controls dynamically
          function generateSystemControls() {
            const container = document.getElementById('system-controls-container');
            if (!container) return;

            // Get user level from body data attribute or from system state
            let userLevel = 0;
            if (window.bambiSystem) {
              userLevel = window.bambiSystem.getUserLevel();
            } else {
              userLevel = parseInt(document.body.getAttribute('data-level') || '0');
            }

            // Create control structure
            const structure = `
              <div class="system-controls" data-user-level="${userLevel}">
                <!-- Control buttons section -->
                <div id="buttons" class="control-buttons">
                  <!-- System control buttons will be added here -->
                </div>
                
                <!-- Control panels section -->
                <div id="console" class="control-panels">
                  <!-- System control panels will be added here -->
                </div>
                
                <!-- XP progress bar -->
                <div class="xp-progress-container">
                  <div class="xp-level-badge">
                    <span id="level-indicator">Level ${userLevel}</span>
                  </div>
                  <div class="xp-progress-label" id="xp-progress-label">
                    Loading XP...
                  </div>
                  <div class="xp-progress-bar">
                    <div class="xp-progress-fill" id="xp-progress-fill" style="width: 0%"></div>
                  </div>
                </div>
              </div>
            `;

            container.innerHTML = structure;

            // After DOM is updated, initialize the controls
            setTimeout(() => {
              if (window.bambiSystem) {
                // This will trigger all the controls to initialize
                document.dispatchEvent(new CustomEvent('system-initialized'));
              }
            }, 100);
          }
        </script>
        <script>
          // Monitor connection status
          (function () {
            let connectionMonitor = null;

            function startConnectionMonitoring() {
              if (connectionMonitor) clearInterval(connectionMonitor);

              connectionMonitor = setInterval(() => {
                if (window.socket) {
                  if (!window.socket.connected) {
                    console.warn('Socket disconnected - attempting to reconnect');
                    if (window.socketHandler && typeof window.socketHandler.reconnect === 'function') {
                      window.socketHandler.reconnect();
                    }
                  }
                } else {
                  console.error('Socket not initialized');
                  if (window.socketHandler && typeof window.socketHandler.init === 'function') {
                    window.socketHandler.init();
                  }
                }
              }, 30000); // Check every 30 seconds
            }

            // Start monitoring once page is fully loaded
            window.addEventListener('load', () => {
              setTimeout(startConnectionMonitoring, 2000);
            });

            // Store function to expose for manual checks
            window.checkConnection = function () {
              if (window.socket && window.socket.connected) {
                console.log('Socket connection is active');
                return true;
              } else {
                console.warn('Socket connection is inactive');
                if (window.socketHandler && typeof window.socketHandler.reconnect === 'function') {
                  window.socketHandler.reconnect();
                }
                return false;
              }
            };
          })();
        </script>
        <script>
          document.addEventListener('DOMContentLoaded', function() {
            // Initial creation of control panel
            if (window.systemControlUI && typeof window.systemControlUI.createControlPanel === 'function') {
              window.systemControlUI.createControlPanel();
            }
            
            // Initialize the UI module
            if (window.systemControlUI && typeof window.systemControlUI.init === 'function') {
              window.systemControlUI.init();
            }
            
            // Ensure controls are visible after a short delay to allow other modules to initialize
            setTimeout(function() {
              if (window.systemControlUI && typeof window.systemControlUI.ensureControlsVisibility === 'function') {
                window.systemControlUI.ensureControlsVisibility();
              }
            }, 500);
          });
        </script>
        <script>
          window.bambiSessions = (function() {
            // Private variables
            let currentSessionId = null;
            let isSessionDirty = false;
            let sessionMessages = [];
            
            // Initialize module
            function init() {
              try {
                // Set up event listeners
                setupEventListeners();
                
                console.log('Session manager initialized');
              } catch (error) {
                console.error('Error initializing session manager:', error);
              }
            }
            
            // Set up event listeners
            function setupEventListeners() {
              if (window.socket) {
                // Session events from server
                window.socket.on('session-loaded', handleSessionLoaded);
                window.socket.on('session-saved', handleSessionSaved);
                window.socket.on('session-created', handleSessionCreated);
                window.socket.on('session-deleted', handleSessionDeleted);
                window.socket.on('session-error', handleSessionError);
                window.socket.on('sessions-list', handleSessionsList);
                
                // Socket lifecycle events
                window.socket.on('connect', handleConnect);
                window.socket.on('disconnect', handleDisconnect);
              }
              
              // Listen for system updates that should be saved
              document.addEventListener('trigger-state-changed', markSessionDirty);
              document.addEventListener('collar-update', markSessionDirty);
              document.addEventListener('spiral-update', markSessionDirty);
            }
            
            // Mark session as needing to be saved
            function markSessionDirty() {
              isSessionDirty = true;
              
              // Add visual indicator if session has unsaved changes
              const saveIndicator = document.getElementById('session-save-indicator');
              if (saveIndicator) {
                saveIndicator.classList.add('unsaved');
                saveIndicator.textContent = 'Unsaved changes';
              }
            }
            
            // Load a session
            function loadSession(sessionId) {
              if (!sessionId || !window.socket) return;
              
              try {
                // Show loading indicator
                const container = document.getElementById('system-controls-container');
                if (container) container.classList.add('loading');
                
                // Send load request
                window.socket.emit('load-session', sessionId);
              } catch (error) {
                console.error('Error loading session:', error);
                
                // Remove loading indicator
                const container = document.getElementById('system-controls-container');
                if (container) container.classList.remove('loading');
              }
            }
            
            // Save current session
            function saveSession(options = {}) {
              if (!window.socket || !window.username || window.username === 'anonBambi') {
                showSessionError('You must set a BambiName to save sessions');
                return;
              }
              
              try {
                // Collect settings from central state manager
                const settings = collectSettings();
                
                // Get recent messages if needed
                const messageCount = options.messageCount || 50;
                const messages = getRecentMessages(messageCount);
                
                // Create session data object
                const sessionData = {
                  sessionId: currentSessionId,
                  title: options.title || `Session ${new Date().toLocaleString()}`,
                  settings: settings,
                  messages: messages
                };
                
                // Send save request
                window.socket.emit('save-session', sessionData);
                
                // Reset dirty flag
                isSessionDirty = false;
                
                // Update UI
                const saveIndicator = document.getElementById('session-save-indicator');
                if (saveIndicator) {
                  saveIndicator.classList.remove('unsaved');
                  saveIndicator.textContent = 'Saving...';
                }
              } catch (error) {
                console.error('Error saving session:', error);
                showSessionError('Error saving session');
              }
            }
            
            // Delete a session
            function deleteSession(sessionId) {
              if (!sessionId || !window.socket || !window.username || window.username === 'anonBambi') {
                showSessionError('You must set a BambiName to delete sessions');
                return;
              }
              
              try {
                // Show confirmation dialog
                if (!confirm('Are you sure you want to delete this session?')) {
                  return;
                }
                
                // Show loading indicator
                const container = document.getElementById('system-controls-container');
                if (container) container.classList.add('loading');
                
                // Send delete request
                window.socket.emit('delete-session', sessionId);
                
                // Reset current session ID if deleting current session
                if (sessionId === currentSessionId) {
                  currentSessionId = null;
                }
              } catch (error) {
                console.error('Error deleting session:', error);
                
                // Remove loading indicator
                const container = document.getElementById('system-controls-container');
                if (container) container.classList.remove('loading');
                
                // Show error
                showSessionError('Error deleting session');
              }
            }

            // Get list of user sessions
            function getSessionsList() {
              if (!window.socket || !window.username || window.username === 'anonBambi') {
                showSessionError('You must set a BambiName to view sessions');
                return;
              }
              
              try {
                // Send request for sessions list
                window.socket.emit('get-sessions');
              } catch (error) {
                console.error('Error getting sessions list:', error);
                showSessionError('Error fetching sessions');
              }
            }

            // Handle session loaded event
            function handleSessionLoaded(data) {
              try {
                if (!data || !data.session) return;
                
                // Store current session ID
                currentSessionId = data.sessionId;
                
                // Apply session settings
                if (window.bambiSystem) {
                  window.bambiSystem.applySessionSettings({
                    activeTriggers: data.session.metadata?.triggers || [],
                    collarSettings: {
                      enabled: data.session.metadata?.collarActive || false,
                      text: data.session.metadata?.collarText || ''
                    },
                    spiralSettings: data.session.metadata?.spiralSettings || {}
                  });
                }
                
                // Dispatch event for other components
                document.dispatchEvent(new CustomEvent('session-loaded', {
                  detail: { session: data.session }
                }));
                
                // Reset dirty flag
                isSessionDirty = false;
                
                // Remove loading indicator
                const container = document.getElementById('system-controls-container');
                if (container) container.classList.remove('loading');
                
                // Update active session indicator
                updateActiveSessionIndicator(data.session.title);
              } catch (error) {
                console.error('Error handling loaded session:', error);
                
                // Remove loading indicator
                const container = document.getElementById('system-controls-container');
                if (container) container.classList.remove('loading');
              }
            }

            // Handle session saved event
            function handleSessionSaved(data) {
              try {
                if (!data) return;
                
                // Store current session ID
                currentSessionId = data.sessionId;
                
                // Update UI
                const saveIndicator = document.getElementById('session-save-indicator');
                if (saveIndicator) {
                  saveIndicator.classList.remove('unsaved');
                  saveIndicator.textContent = 'Saved';
                  
                  // Hide the text after a delay
                  setTimeout(() => {
                    if (saveIndicator) {
                      saveIndicator.textContent = '';
                    }
                  }, 3000);
                }
                
                // Update active session indicator
                updateActiveSessionIndicator(data.title);
                
                // Refresh sessions list if available
                getSessionsList();
              } catch (error) {
                console.error('Error handling saved session:', error);
              }
            }

            // Handle session created event
            function handleSessionCreated(data) {
              try {
                if (!data) return;
                
                // Store current session ID
                currentSessionId = data.sessionId;
                
                // Update UI
                const saveIndicator = document.getElementById('session-save-indicator');
                if (saveIndicator) {
                  saveIndicator.classList.remove('unsaved');
                  saveIndicator.textContent = 'Created';
                  
                  // Hide the text after a delay
                  setTimeout(() => {
                    if (saveIndicator) {
                      saveIndicator.textContent = '';
                    }
                  }, 3000);
                }
                
                // Update active session indicator
                updateActiveSessionIndicator(data.title);
                
                // Refresh sessions list if available
                getSessionsList();
              } catch (error) {
                console.error('Error handling created session:', error);
              }
            }

            // Handle session deleted event
            function handleSessionDeleted(data) {
              try {
                // Remove loading indicator
                const container = document.getElementById('system-controls-container');
                if (container) container.classList.remove('loading');
                
                // If this was the active session, update UI
                if (data.sessionId === currentSessionId) {
                  currentSessionId = null;
                  updateActiveSessionIndicator('');
                }
                
                // Refresh sessions list
                getSessionsList();
              } catch (error) {
                console.error('Error handling session deletion:', error);
              }
            }

            // Handle session error event
            function handleSessionError(data) {
              // Remove loading indicator
              const container = document.getElementById('system-controls-container');
              if (container) container.classList.remove('loading');
              
              // Show error message
              showSessionError(data.message || 'Error processing session');
            }

            // Handle sessions list event
            function handleSessionsList(data) {
              try {
                if (!data || !data.sessions) return;
                
                // Update UI with sessions list
                const sessionsContainer = document.getElementById('sessions-container');
                if (!sessionsContainer) return;
                
                // Clear existing list
                sessionsContainer.innerHTML = '';
                
                if (data.sessions.length === 0) {
                  sessionsContainer.innerHTML = '<p>No saved sessions found</p>';
                  return;
                }
                
                // Create list of sessions
                const sessionsList = document.createElement('ul');
                sessionsList.className = 'sessions-list';
                
                data.sessions.forEach(session => {
                  const sessionItem = document.createElement('li');
                  sessionItem.className = 'session-item';
                  if (session._id === currentSessionId) {
                    sessionItem.classList.add('active');
                  }
                  
                  // Create session title
                  const title = document.createElement('span');
                  title.className = 'session-title';
                  title.textContent = session.title || 'Untitled Session';
                  
                  // Create date element
                  const date = document.createElement('span');
                  date.className = 'session-date';
                  date.textContent = new Date(session.updatedAt || session.createdAt).toLocaleString();
                  
                  // Create load button
                  const loadBtn = document.createElement('button');
                  loadBtn.className = 'session-load-btn';
                  loadBtn.textContent = 'Load';
                  loadBtn.addEventListener('click', () => loadSession(session._id));
                  
                  // Create delete button
                  const deleteBtn = document.createElement('button');
                  deleteBtn.className = 'session-delete-btn';
                  deleteBtn.textContent = 'Delete';
                  deleteBtn.addEventListener('click', () => deleteSession(session._id));
                  
                  // Add elements to session item
                  sessionItem.appendChild(title);
                  sessionItem.appendChild(date);
                  sessionItem.appendChild(loadBtn);
                  sessionItem.appendChild(deleteBtn);
                  
                  // Add session item to list
                  sessionsList.appendChild(sessionItem);
                });
                
                sessionsContainer.appendChild(sessionsList);
              } catch (error) {
                console.error('Error handling sessions list:', error);
              }
            }

            // Handle socket connection event
            function handleConnect() {
              console.log('Socket connected, refreshing sessions');
              
              // Fetch sessions list if user is logged in
              if (window.username && window.username !== 'anonBambi') {
                getSessionsList();
              }
            }

            // Handle socket disconnection event
            function handleDisconnect() {
              console.log('Socket disconnected, session operations unavailable');
              
              // Update UI to show disconnected state
              const sessionsContainer = document.getElementById('sessions-container');
              if (sessionsContainer) {
                sessionsContainer.innerHTML = '<p>Disconnected from server. Session operations unavailable.</p>';
              }
            }

            // Get recent chat messages
            function getRecentMessages(count = 50) {
              try {
                // Check for chat messages in DOM
                const chatMessages = [];
                const messageElements = document.querySelectorAll('#chat-response li');
                
                // Get the most recent messages up to count
                const start = Math.max(0, messageElements.length - count);
                for (let i = start; i < messageElements.length; i++) {
                  const el = messageElements[i];
                  const username = el.querySelector('.chat-username a')?.textContent?.trim() || 'unknown';
                  const message = el.querySelector('.chat-message')?.textContent?.trim() || '';
                  const time = el.querySelector('.chat-time')?.textContent?.trim() || '';
                  
                  if (username && message) {
                    chatMessages.push({
                      username,
                      message,
                      timestamp: time
                    });
                  }
                }
                
                return chatMessages;
              } catch (error) {
                console.error('Error getting chat messages:', error);
                return [];
              }
            }

            // Update active session indicator in UI
            function updateActiveSessionIndicator(sessionTitle) {
              try {
                const sessionIndicator = document.getElementById('active-session-indicator');
                if (!sessionIndicator) return;
                
                if (sessionTitle) {
                  sessionIndicator.textContent = `Active session: ${sessionTitle}`;
                  sessionIndicator.style.display = 'block';
                } else {
                  sessionIndicator.textContent = '';
                  sessionIndicator.style.display = 'none';
                }
              } catch (error) {
                console.error('Error updating session indicator:', error);
              }
            }

            // Show session error message
            function showSessionError(message) {
              try {
                // Look for toast notification system first
                if (window.showToast) {
                  window.showToast(message, 'error');
                  return;
                }
                
                // Fallback to alert
                alert(message);
              } catch (error) {
                console.error('Error showing session error:', error);
                alert(message);
              }
            }

            // Get active triggers from DOM or system
            function getActiveTriggers() {
              try {
                // Try to get from central system first
                if (window.bambiSystem && typeof window.bambiSystem.getActiveTriggers === 'function') {
                  return window.bambiSystem.getActiveTriggers();
                }
                
                // Fallback to DOM elements
                return Array.from(document.querySelectorAll('.trigger-toggle:checked, .trigger.active'))
                  .map(el => el.getAttribute('data-trigger-name') || el.getAttribute('data-trigger'))
                  .filter(Boolean);
              } catch (error) {
                console.error('Error getting active triggers:', error);
                return [];
              }
            }

            // Get collar settings from DOM or system
            function getCollarSettings() {
              try {
                // Try to get from central system first
                if (window.bambiSystem && typeof window.bambiSystem.getCollarSettings === 'function') {
                  return window.bambiSystem.getCollarSettings();
                }
                
                // Fallback to DOM elements
                const collarEnabled = document.querySelector('#collar-enabled')?.checked || false;
                const collarText = document.querySelector('#collar-text')?.value || '';
                
                return {
                  enabled: collarEnabled,
                  text: collarText
                };
              } catch (error) {
                console.error('Error getting collar settings:', error);
                return { enabled: false, text: '' };
              }
            }

            // Get spiral settings from DOM or system
            function getSpiralSettings() {
              try {
                // Try to get from central system first
                if (window.bambiSystem && typeof window.bambiSystem.getSpiralSettings === 'function') {
                  return window.bambiSystem.getSpiralSettings();
                }
                
                // Default values if not found
                return {
                  enabled: false,
                  spiral1Width: 20,
                  spiral2Width: 20,
                  spiral1Speed: 10,
                  spiral2Speed: 7
                };
              } catch (error) {
                console.error('Error getting spiral settings:', error);
                return { enabled: false };
              }
            }

            // Collect settings from all system components
            function collectSettings() {
              try {
                // Try to use central state management first
                if (window.bambiSystem && typeof window.bambiSystem.collectSettings === 'function') {
                  return window.bambiSystem.collectSettings();
                }
                
                // Fallback to direct collection
                return {
                  activeTriggers: getActiveTriggers(),
                  collarSettings: getCollarSettings(),
                  spiralSettings: getSpiralSettings()
                };
              } catch (error) {
                console.error('Error collecting settings:', error);
                return {
                  activeTriggers: [],
                  collarSettings: { enabled: false, text: '' },
                  spiralSettings: { enabled: false }
                };
              }
            }

            // Public API
            return {
              init,
              loadSession,
              saveSession,
              deleteSession,
              getSessionsList,
              getCurrentSessionId: () => currentSessionId,
              isSessionDirty: () => isSessionDirty,
              collectSettings
            };
          })();

          // Initialize on page load
          document.addEventListener('DOMContentLoaded', function() {
            if (window.bambiSessions) {
              window.bambiSessions.init();
            }
          });
        </script>
        <script>
          (function() {
            // Simple function to ensure controls are visible
            function forceControlsVisibility() {
              try {
                // Target the system controls container
                const container = document.getElementById('system-controls-container');
                if (container) {
                  // Make sure it's visible
                  container.style.display = 'block';
                  container.style.visibility = 'visible';
                  container.style.opacity = '1';
                  
                  // Check if its height is collapsed
                  if (container.offsetHeight < 50) {
                    container.style.minHeight = '150px';
                  }
                }
                
                // Target the XP components specifically
                const xpContainer = document.querySelector('.xp-progress-container');
                if (xpContainer) {
                  xpContainer.style.display = 'flex';
                  xpContainer.style.visibility = 'visible';
                  xpContainer.style.opacity = '1';
                }
                
                // Make the level indicator visible
                const levelIndicator = document.getElementById('level-indicator');
                if (levelIndicator) {
                  levelIndicator.style.display = 'block';
                  levelIndicator.style.visibility = 'visible';
                  levelIndicator.style.opacity = '1';
                }
                
                // Fill bar
                const xpFill = document.getElementById('xp-progress-fill');
                if (xpFill) {
                  xpFill.style.display = 'block';
                  xpFill.style.width = '20%'; // Default to something visible
                }
                
                // Check if console div and buttons divs are present
                // Rename variable to avoid conflict with global console object
                const consoleDiv = document.getElementById('console'); 
                const buttons = document.getElementById('buttons');
                
                if (consoleDiv) {
                  consoleDiv.style.display = 'block';
                  consoleDiv.style.minHeight = '50px';
                }
                
                if (buttons) {
                  buttons.style.display = 'flex';
                  buttons.style.minHeight = '40px';
                }
                
              } catch (error) {
                // Use window.console to make it explicit
                window.console.error('Error forcing controls visibility:', error);
              }
            }
            
            // Run immediately
            forceControlsVisibility();
            
            // Also run after delays to ensure DOM is fully rendered
            setTimeout(forceControlsVisibility, 1000);
            setTimeout(forceControlsVisibility, 3000);
            
            // Initialize minimal bambiSystem if needed
            if (!window.bambiSystem) {
              // Create minimal implementation
              window.bambiSystem = {
                getUserLevel: function() { return 1; },
                getActiveTriggers: function() { return []; },
                getCollarSettings: function() { return { enabled: false, text: '' }; },
                getSpiralSettings: function() { return { enabled: false }; },
                collectSettings: function() { 
                  return {
                    activeTriggers: [],
                    collarSettings: { enabled: false, text: '' },
                    spiralSettings: { enabled: false }
                  };
                },
                applySessionSettings: function() {}
              };
              
              // Trigger system initialization
              document.dispatchEvent(new CustomEvent('system-initialized'));
            }
            
            // Add to window for troubleshooting
            window.forceControlsVisibility = forceControlsVisibility;
          })();
        </script>
        <script>
          // Fix for JSON storage parsing error
          (function() {
            // Helper to safely parse JSON data
            function safeJsonParse(data, fallback = {}) {
              try {
                // Check if data is already an object
                if (typeof data === 'object' && data !== null) {
                  return data;
                }
                
                // Only parse if we have a string
                if (typeof data === 'string') {
                  return JSON.parse(data);
                }
                
                return fallback;
              } catch (e) {
                console.error('Error parsing JSON data:', e);
                return fallback;
              }
            }
            
            // Patch localStorage getItem to safely handle JSON
            const originalGetItem = localStorage.getItem;
            localStorage.getItem = function(key) {
              const value = originalGetItem.call(this, key);
              
              // Keys that should be parsed as JSON
              const jsonKeys = [
                'bambiSystemState',
                'sessionSettings',
                'triggerState',
                'collarSettings',
                'spiralSettings',
                'audioSettings'
              ];
              
              // Only attempt JSON parsing if the key is in our list
              if (value && jsonKeys.includes(key)) {
                try {
                  return safeJsonParse(value);
                } catch (e) {
                  console.warn(`Failed to parse ${key} as JSON, returning raw value`);
                }
              }
              
              return value;
            };
            
            // Override storage event handlers to ensure proper format
            window.addEventListener('storage', function(e) {
              // Protect against non-JSON values in expected JSON keys
              const jsonKeys = [
                'bambiSystemState',
                'sessionSettings',
                'triggerState',
                'collarSettings',
                'spiralSettings',
                'audioSettings'
              ];
              
              if (jsonKeys.includes(e.key) && e.newValue) {
                try {
                  // Test if value can be parsed
                  JSON.parse(e.newValue);
                } catch (err) {
                  console.warn(`Invalid JSON in storage for key ${e.key}, reverting to previous value`);
                  // Prevent the invalid data from being processed further
                  e.stopImmediatePropagation();
                }
              }
            }, true); // Use capture to handle event before other listeners
            
            // Fix for storage handlers in content.js
            if (window._storageChangeDispatcher) {
              const originalDispatcher = window._storageChangeDispatcher;
              window._storageChangeDispatcher = function(e) {
                try {
                  // Only process if newValue exists and is valid
                  if (!e.newValue) return originalDispatcher.call(this, e);
                  
                  // If value should be JSON, make sure it's valid first
                  const jsonKeys = [
                    'bambiSystemState',
                    'sessionSettings',
                    'triggerState', 
                    'collarSettings',
                    'spiralSettings',
                    'audioSettings'
                  ];
                  
                  if (jsonKeys.includes(e.key)) {
                    // Test parse before passing to handler
                    JSON.parse(e.newValue);
                  }
                  
                  return originalDispatcher.call(this, e);
                } catch (err) {
                  console.warn(`Storage change handler prevented error for ${e.key}`);
                }
              };
            }
          })();

          // Add this to your existing JSON storage parsing error fix script
          (function() {
            // Fix specifically for system-controls.js loadFromStorage
            const originalSystemControlsLoadFromStorage = window.loadFromStorage;
            
            if (typeof originalSystemControlsLoadFromStorage === 'function') {
              window.loadFromStorage = function(key, defaultValue = {}) {
                try {
                  const value = localStorage.getItem(key);
                  
                  // No value stored yet
                  if (!value) return defaultValue;
                  
                  // Check if already an object (might be from our patched localStorage.getItem)
                  if (typeof value === 'object' && value !== null) {
                    return value;
                  }
                  
                  // Otherwise try to parse it
                  return JSON.parse(value);
                } catch (err) {
                  console.warn(`Error loading state for ${key}, using default`, err);
                  return defaultValue;
                }
              };
            }
            
            // Fix for system state setters
            const originalSetState = window.setState;
            
            if (typeof originalSetState === 'function') {
              window.setState = function(key, value) {
                try {
                  // Make sure we're stringifying objects before storage
                  if (typeof value === 'object' && value !== null) {
                    localStorage.setItem(key, JSON.stringify(value));
                  } else {
                    localStorage.setItem(key, value);
                  }
                  
                  // Dispatch event for any listeners
                  window.dispatchEvent(new CustomEvent('state-change', {
                    detail: { key, value }
                  }));
                  
                } catch (err) {
                  console.warn(`Error saving state for ${key}`, err);
                }
              };
            }
            
            // Patch for system-controls.js initialization
            document.addEventListener('DOMContentLoaded', function() {
              // Fix any existing corrupted state
              try {
                const keys = ['bambiSystemState', 'sessionSettings', 'triggerState', 'collarSettings', 'spiralSettings'];
                
                keys.forEach(key => {
                  const rawValue = localStorage.getItem(key);
                  
                  // Check if the value is stored as an object instead of a string
                  if (rawValue === "[object Object]") {
                    console.warn(`Found corrupted state for ${key}, resetting`);
                    localStorage.removeItem(key);
                  }
                });
              } catch (err) {
                console.error('Error fixing corrupted state:', err);
              }
            });
          })();
        </script>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/p5@1.4.0/lib/p5.js"></script>
        <script src="js/bootstrap.min.js"></script>
  </body>

</html>