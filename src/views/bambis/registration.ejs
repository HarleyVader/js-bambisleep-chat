<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bambi Forest - Register</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <h1 class="neon-glow">Join the church of bambisleep</h1>
    
    <div class="card">
      <h2>Create Your Bambi Account</h2>
      
      <% if (error) { %>
        <div class="error-message" style="background-color: var(--error-bg); color: var(--error); padding: 10px; margin-bottom: 20px; border-radius: 4px;">
          <%= error %>
        </div>
      <% } %>
      
      <form action="/register" method="POST">
        <div class="input-container">
          <label for="username">Username</label>
          <input type="text" id="username" name="username" required>
        </div>
        
        <div class="input-container">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" required>
        </div>
        
        <div class="input-container">
          <label for="displayName">Display Name</label>
          <input type="text" id="displayName" name="displayName">
        </div>
        
        <button type="submit">Create Account</button>
      </form>
      
      <p style="text-align: center; margin-top: 20px;">
        Already have an account? <a href="/login">Enter the church</a>
      </p>
    </div>
  </div>
</body>
</html>